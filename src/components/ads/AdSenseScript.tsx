/**
 * Ad units still load only after cookie consent (see AdPlacement).
 * The global AdSense script is injected in layout <head> for site verification.
 */
export function AdSenseScript() {
  return null;
}
