/** @deprecated Import from `@/lib/transit-stations` — kept for backward compatibility. */
export {
  BTS_LOCATIONS,
  DEFAULT_MAP_CENTER,
  NEARBY_STATIONS,
  STATION_CATEGORY_ORDER,
  findStationId,
  formatNearbyStation,
  getGoogleMapsUrl,
  getOsmEmbedUrl,
  getStationById,
  getStationCoords,
  stationsByCategory,
  type NearbyStation,
  type StationCategory,
} from "@/lib/transit-stations";
